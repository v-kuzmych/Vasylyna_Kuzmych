management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      show-details: always

info:
  app:
    name: Cinema
    description: This is an application for cinemas
    version: 1.0.0

spring:
  jpa:
    show-sql: true
    database: MYSQL
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cinema?useUnicode=true&serverTimezone=UTC
    username: root
    password: password
